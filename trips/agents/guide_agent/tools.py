"""The guide currently uses ADK's built-in Google Search tool.

Trip data is supplied as authoritative context by the application instead of being
looked up by a model-callable tool, which prevents access to another user's trip.
"""
